# 维护脚本测试集

版本：v1.0（2026-09-10）

对象：仓库根 `scripts/` 下 5 个脚本（`lib.sh` 为被 source 的公共库，随各脚本覆盖）。标准与行为定义见 [开发与维护规范.md](开发与维护规范.md) §6/§8。涉及临时改动的用例（L-2/L-3/L-5、D-2/D-3、N-1~N-4）执行后必须还原，还原后以 L-1 全绿收尾。

## 1. lint 场景（L）

### L-1 全绿基线

场景：仓库处于规范合规状态。

▶ `bash scripts/skills-lint.sh`

- = 输出含每个 skill 的 ok 行，末尾 `lint: N ok, 0 warn, 0 error`
- = exit 0

### L-2 台账漂移检出

场景：SKILLS.md 总览表临时追加一行虚构 skill（如 `ghost-skill`）。

▶ `bash scripts/skills-lint.sh`

- = ERROR：台账总览的「ghost-skill」在磁盘上不存在 + 台账明细缺少小节
- = exit 1；还原后 L-1 恢复全绿

### L-3 版本失配检出

场景：临时把某 skill（如 theme-commit）frontmatter 的 `metadata.version` 改为 `9.9.9`。

▶ `bash scripts/skills-lint.sh`

- = ERROR：台账总览版本「0.1.0」与 frontmatter「9.9.9」不一致
- = exit 1；还原后恢复

### L-4 name 失配检出

场景：临时把某 skill frontmatter 的 `name` 改为与目录名不同的值。

▶ `bash scripts/skills-lint.sh`

- = ERROR：frontmatter name 与目录名不一致
- = exit 1；还原后恢复

### L-5 运行产物入库检出

场景：`mkdir -p ddgs-web-access/.venv && touch ddgs-web-access/.venv/x && git add -f ddgs-web-access/.venv/x`。

▶ `bash scripts/skills-lint.sh`

- = ERROR：运行产物已入库：ddgs-web-access/.venv/x
- = exit 1；`git rm --cached` + 删除文件还原后恢复

## 2. doctor 场景（D）

### D-1 健康矩阵

场景：`skills-link.sh --all` 后。

▶ `bash scripts/skills-doctor.sh`

- = 全部现存 agent 目录列均为 linked
- = 末尾 `doctor: N linked, 0 missing, 0 foreign, 0 broken`，exit 0

### D-2 broken 检出

场景：在某个 agent skills 目录手工建一个指向不存在路径的 junction（`cmd /c mklink /J <agent>/skills/<skill> C:\nonexistent\<skill>`）。

▶ `bash scripts/skills-doctor.sh`

- = 该格显示 broken，stderr 有 `ERROR: broken: <skill> @ <dir>`
- = exit 1；删掉该 junction 还原后恢复

### D-3 foreign 识别

场景：在某个 agent skills 目录把某 skill 换成指向其他真实目录的链接（或真实拷贝目录）。

▶ `bash scripts/skills-doctor.sh`

- = 该格显示 foreign，exit 0（warn 不算失败）
- = 还原（删掉后重跑 skills-link.sh）后恢复

## 3. link / unlink 场景（K）

### K-1 幂等

▶ `bash scripts/skills-link.sh --all` 连续执行两次

- = 第二次全部 `skip (already linked)`，无新建、无报错
- = 两次均 exit 0

### K-2 指名单链接

▶ `bash scripts/skills-link.sh theme-commit`

- = 只动 theme-commit 相关链接；未安装的 agent 目录自动跳过
- = doctor 中 theme-commit 行全部 linked

### K-3 安全卸载（不误删拷贝）

场景：某 agent skills 目录下放一个该 skill 的**真实拷贝**目录（非链接）。

▶ `bash scripts/skills-unlink.sh --all`

- = 拷贝目录 `skip (not pointing to this repo)`，不被删除
- = 指向本仓库的链接全部 `removed:`

### K-4 未知名称拒绝

▶ `bash scripts/skills-link.sh not-a-skill`

- = stderr `ERROR: 不是本仓库的 skill：not-a-skill`，exit 2，无任何链接动作

## 4. new 场景（N）

### N-1 骨架合法且触发台账校验

▶ `bash scripts/skills-new.sh test-skill` 后 `bash scripts/skills-lint.sh`

- = test-skill 自身的 frontmatter / version / description 检查通过
- = ERROR：台账总览缺少该 skill 的行（L-ledger 生效），exit 1

### N-2 拒绝重名

▶ 再次 `bash scripts/skills-new.sh test-skill`

- = stderr 报目录已存在，exit 2

### N-3 拒绝非法名称

▶ `bash scripts/skills-new.sh Bad_Name`（及 `foo-`、`-foo`）

- = stderr 报 kebab-case 约束，exit 2

### N-4 还原

▶ `rm -rf test-skill` 后 `bash scripts/skills-lint.sh`

- = 恢复 L-1 全绿，exit 0

## 5. 结果记录与回归策略

| 用例 | 首轮结果 | 日期 | 备注 |
| --- | --- | --- | --- |
| L-1 | ✅ | 2026-09-10 | 全绿基线（存量改造完成后） |
| L-2 | ✅ | 2026-09-10 | research-with-docs 漂移即属此类 |
| L-3 | ✅ | 2026-09-10 | — |
| L-4 | ✅ | 2026-09-10 | — |
| L-5 | ✅ | 2026-09-10 | — |
| D-1 | ✅ | 2026-09-10 | link --all 后验证 |
| D-2 | ✅ | 2026-09-10 | 悬空 junction 检出 |
| D-3 | ✅ | 2026-09-10 | — |
| K-1~K-4 | ✅ | 2026-09-10 | 幂等 / 单链 / 安全 / 拒绝 |
| N-1~N-4 | ✅ | 2026-09-10 | 骨架 + 台账联动 + 还原 |

回归策略：改动 `scripts/` 任一脚本后，按上表跑受影响类别的用例；D-2/D-3、L-5 等破坏性用例只在必要时执行，执行后必须还原并以 L-1 收尾。
